---
phase: 18-ticket-detection-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/importer/ticket-scorer.js
  - src/importer/index.js
autonomous: true

must_haves:
  truths:
    - "After re-import, sessions with ticket references only in git commit messages show the correct ticket as primary"
    - "After re-import, sessions with MCP tool calls to Atlassian/Linear/GitHub referencing tickets show those tickets in scoring"
    - "New git_commit and mcp_tool sources do not produce false positives for denylist patterns"
  artifacts:
    - path: "src/importer/ticket-scorer.js"
      provides: "git_commit and mcp_tool scoring logic"
      contains: "git_commit"
      exports: ["scoreTickets", "MCP_TICKET_PREFIXES"]
    - path: "src/importer/index.js"
      provides: "git_commit and mcp_tool detection in detectTicketsFromMessage()"
      contains: "mcp_tool"
  key_links:
    - from: "src/importer/index.js"
      to: "src/importer/ticket-scorer.js"
      via: "import of TICKET_PATTERN, TICKET_PREFIX_DENYLIST, MCP_TICKET_PREFIXES"
      pattern: "import.*MCP_TICKET_PREFIXES"
    - from: "detectTicketsFromMessage()"
      to: "collectTickets()"
      via: "git_commit and mcp_tool source entries flow into tickets table"
      pattern: "source: 'git_commit'|source: 'mcp_tool'"
    - from: "scoreTickets()"
      to: "primary_ticket determination"
      via: "addScore with git_commit (100 base + 10/additional) and mcp_tool (100 base + 10/additional)"
      pattern: "gitCommit|mcpTool"
---

<objective>
Add git commit message and MCP tool call ticket detection to the import pipeline.

Purpose: Sessions that reference tickets only via git commits or MCP tool calls (not branch names or slash commands) currently miss ticket detection entirely. This adds two new message-level detection sources following the existing dual-modification pattern (detection in index.js + scoring in ticket-scorer.js).

Output: Updated ticket-scorer.js with git_commit and mcp_tool scoring, updated index.js with git_commit and mcp_tool detection in detectTicketsFromMessage().
</objective>

<execution_context>
@/home/meckert/.claude/get-shit-done/workflows/execute-plan.md
@/home/meckert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-ticket-detection-pipeline/18-RESEARCH.md

@src/importer/ticket-scorer.js
@src/importer/index.js
@src/importer/parser.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add git commit and MCP tool detection to detectTicketsFromMessage()</name>
  <files>src/importer/index.js, src/importer/ticket-scorer.js</files>
  <action>
In `src/importer/ticket-scorer.js`:
- Export a new constant `MCP_TICKET_PREFIXES = ['atlassian', 'linear', 'github', 'tickets']` (module-level, alongside existing exports like TICKET_PATTERN)

In `src/importer/index.js`:
- Import `MCP_TICKET_PREFIXES` from `./ticket-scorer.js`

Add two new detection blocks to `detectTicketsFromMessage(msg)`:

**Block 1 — Git commit messages (inside the existing `if (msg.type === 'user')` block):**
- Check `msg.rawMessage?.message?.content` — if it's an Array, iterate over blocks
- For blocks with `type === 'tool_result'`:
  - Handle content as string directly, OR if content is an array of text blocks, join their text fields
  - Match git commit output pattern: `/\[[^\]]+\s+[0-9a-f]{7,}\]\s+(.+?)(?:\n|$)/g`
  - For each commit match, extract the commit message (capture group 1) and scan it with `TICKET_PATTERN.matchAll()`
  - Filter with TICKET_PREFIX_DENYLIST
  - Push results with `source: 'git_commit'` and `detected_at: msg.timestamp`

**Block 2 — MCP tool call inputs (new block for `msg.type === 'assistant'`):**
- Check `msg.rawMessage?.message?.content` — if it's an Array, iterate over blocks
- For blocks with `type === 'tool_use'` where `block.name` starts with `'mcp__'`:
  - Extract server name: split `block.name` on `'__'`, take `parts[1]`
  - Check if server starts with any prefix in `MCP_TICKET_PREFIXES` (use `.some(p => server.startsWith(p))`)
  - If match: `JSON.stringify(block.input || {})` and scan with `TICKET_PATTERN.matchAll()`
  - Filter with TICKET_PREFIX_DENYLIST
  - Push results with `source: 'mcp_tool'` and `detected_at: msg.timestamp`

Important: Use `matchAll()` (not `exec()` or `test()`) to avoid `/g` flag lastIndex issues. The existing code already follows this pattern.
  </action>
  <verify>
Run `node -e "import('./src/importer/index.js').then(() => console.log('index.js loads OK'))"` and `node -e "import('./src/importer/ticket-scorer.js').then(m => { console.log('MCP_TICKET_PREFIXES:', m.MCP_TICKET_PREFIXES); console.log('scoreTickets:', typeof m.scoreTickets) })"` to confirm both files parse and export correctly.
  </verify>
  <done>detectTicketsFromMessage() returns git_commit and mcp_tool source entries for messages containing git commit output or MCP tool_use blocks with ticket references. MCP_TICKET_PREFIXES is exported from ticket-scorer.js.</done>
</task>

<task type="auto">
  <name>Task 2: Add git commit and MCP tool scoring to scoreTickets()</name>
  <files>src/importer/ticket-scorer.js</files>
  <action>
In `scoreTickets()` in `src/importer/ticket-scorer.js`, add two new scoring blocks in the per-message loop:

**Block 1 — Git commit scoring (inside per-message loop):**
- Before the loop, declare `const gitCommitBaseSeen = new Set()` to track which tickets have received their 100pt base
- In the loop, for `msg.type === 'user'` messages:
  - Check `msg.rawMessage?.message?.content` — if Array, iterate blocks
  - For `tool_result` blocks: handle content as string or array-of-text-blocks (same as Task 1)
  - Match git commit output pattern: `/\[[^\]]+\s+[0-9a-f]{7,}\]\s+(.+?)(?:\n|$)/g`
  - For each commit match, scan commit message text with `TICKET_PATTERN.matchAll()`
  - For each ticket found: if `gitCommitBaseSeen` does NOT have the uppercase key, `addScore(ticket, 100)` and add to set; otherwise `addScore(ticket, 10)`
  - This gives 100pts for first commit mentioning a ticket, +10pts per additional commit

**Block 2 — MCP tool call scoring (new block in per-message loop):**
- Before the loop, declare `const mcpToolBaseSeen = new Set()` to track base scoring
- In the loop, for `msg.type === 'assistant'` messages:
  - Check `msg.rawMessage?.message?.content` — if Array, iterate blocks
  - For `tool_use` blocks where `block.name` starts with `'mcp__'`:
    - Extract server name, check against `MCP_TICKET_PREFIXES` (same logic as Task 1)
    - `JSON.stringify(block.input || {})` and scan with `TICKET_PATTERN.matchAll()`
    - For each ticket: if `mcpToolBaseSeen` does NOT have uppercase key, `addScore(ticket, 100)` and add to set; otherwise `addScore(ticket, 10)`

**Update the JSDoc comment** on `scoreTickets()` to document the new scoring weights:
- Git commit message: 100 pts base, +10 per additional commit
- MCP tool call input: 100 pts base, +10 per additional call

Note: The `addScore()` closure already handles denylist filtering and uppercase normalization. Do NOT duplicate that logic.

Note: The git commit and MCP parsing logic here intentionally parallels the detection logic in `detectTicketsFromMessage()` (Task 1). This is the existing architecture pattern — `detectTicketsFromMessage()` populates the `tickets` table (source tracking), while `scoreTickets()` independently computes the primary ticket score. Both need to parse the same message structures. Add a brief comment in scoreTickets() noting this intentional parallel: `// Parallel parsing with detectTicketsFromMessage() — intentional; detection populates tickets table, scoring determines primary_ticket`
  </action>
  <verify>
Run `node -e "import('./src/importer/ticket-scorer.js').then(m => console.log('scoreTickets loads:', typeof m.scoreTickets))"` to confirm the file parses. Then run `npm start` briefly and trigger a re-import to verify no runtime errors (Ctrl+C after import completes).
  </verify>
  <done>scoreTickets() awards 100pts base + 10pts/additional for git_commit tickets, and 100pts base + 10pts/additional for mcp_tool tickets. Both use Set-based tracking to distinguish first vs additional detections. Comment documents intentional parallel with detectTicketsFromMessage().</done>
</task>

</tasks>

<verification>
1. Both `src/importer/ticket-scorer.js` and `src/importer/index.js` load without errors
2. `MCP_TICKET_PREFIXES` is exported from ticket-scorer.js
3. A full import (`npm start` then trigger import) completes without errors
4. Existing ticket detection (slash_command, content, branch) continues to work unchanged
</verification>

<success_criteria>
- detectTicketsFromMessage() produces `source: 'git_commit'` entries for messages containing `[branch hash] commit message` patterns in tool_result blocks
- detectTicketsFromMessage() produces `source: 'mcp_tool'` entries for assistant messages containing tool_use blocks from configured MCP prefixes
- scoreTickets() awards correct points: git_commit 100 base + 10/additional, mcp_tool 100 base + 10/additional
- Denylist filtering applies to all new sources (existing TICKET_PREFIX_DENYLIST)
- No regressions in existing slash_command, content, or branch detection/scoring
</success_criteria>

<output>
After completion, create `.planning/phases/18-ticket-detection-pipeline/18-01-SUMMARY.md`
</output>
