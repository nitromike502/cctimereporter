---
phase: 12-tour-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/client/pages/TimelinePage.vue]
autonomous: true

must_haves:
  truths:
    - "Guided tour includes a step highlighting the project filter checkboxes when multiple projects exist"
    - "Guided tour includes a step highlighting the day summary totals section"
    - "When only one project exists, the filter-bar tour step is skipped (no broken centered popover)"
    - "Tour step order is logical: date picker -> import -> filter (conditional) -> gantt -> session detail -> day summary"
  artifacts:
    - path: "src/client/pages/TimelinePage.vue"
      provides: "Updated startTourIfNew() with dynamic steps array"
      contains: "filter-bar"
  key_links:
    - from: "startTourIfNew()"
      to: "colorizedProjects.value.length"
      via: "conditional step inclusion"
      pattern: "colorizedProjects\\.value\\.length > 1"
---

<objective>
Add two new tour steps to the existing driver.js guided tour: one for the project filter checkboxes (`.filter-bar`) and one for the day summary totals (`.day-summary`).

Purpose: New users should discover all key UI features during the first-visit tour, not just the top-level controls.
Output: Updated `startTourIfNew()` in TimelinePage.vue with 6 steps (up from 4), filter-bar step conditionally included.
</objective>

<execution_context>
@/home/meckert/.claude/get-shit-done/workflows/execute-plan.md
@/home/meckert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/client/pages/TimelinePage.vue
@.planning/phases/12-tour-enhancements/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filter-bar and day-summary tour steps with conditional logic</name>
  <files>src/client/pages/TimelinePage.vue</files>
  <action>
Refactor `startTourIfNew()` (lines 121-163) to build the `steps` array dynamically instead of passing it inline to `driver()`.

1. Create a `const steps = [...]` array with the existing 4 steps (datepicker-wrapper, import-group, gantt-chart, session-detail-panel) in the same order.

2. After the initial 4 steps, conditionally push the filter-bar step:
   ```javascript
   if (colorizedProjects.value.length > 1) {
     steps.push({
       element: '.filter-bar',
       popover: {
         title: 'Filter by Project',
         description: 'Use these checkboxes to show or hide individual projects in the timeline.',
         side: 'bottom',
       },
     })
   }
   ```
   This is gated because `.filter-bar` has `v-if="colorizedProjects.length > 1"` — when only one project exists, the element is absent from the DOM, and driver.js would show a broken centered popover.

3. Always push the day-summary step last:
   ```javascript
   steps.push({
     element: '.day-summary',
     popover: {
       title: 'Day Totals',
       description: 'See total working time for the day, broken down by project, ticket, and branch.',
       side: 'top',
     },
   })
   ```

4. Pass `steps` to `driver({ showProgress: true, onDestroyed: ..., steps })`.

Important: Do NOT change the TOUR_KEY constant or reset it. Existing users who already completed the tour will not see it again — this is intentional.
  </action>
  <verify>
  Run `npm run build` to confirm no syntax errors. Then grep TimelinePage.vue for:
  - `filter-bar` appears in a tour step
  - `day-summary` appears in a tour step
  - `colorizedProjects.value.length > 1` gates the filter-bar step
  - The `steps` array is passed to `driver()` as a variable (not inline)
  </verify>
  <done>
  `startTourIfNew()` builds a dynamic steps array with 4 base steps + 1 conditional filter-bar step + 1 day-summary step. The filter-bar step is only included when `colorizedProjects.value.length > 1`. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -c "filter-bar\|day-summary" src/client/pages/TimelinePage.vue` shows matches for both selectors in tour steps
3. `grep "colorizedProjects.value.length > 1" src/client/pages/TimelinePage.vue` confirms conditional gating
</verification>

<success_criteria>
- Tour has 5 or 6 steps (6 when multiple projects, 5 when single project)
- Filter-bar step only appears when `.filter-bar` element exists in DOM
- Day summary step always appears as the final tour step
- No regression to existing 4 tour steps
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-tour-enhancements/12-01-SUMMARY.md`
</output>
