---
phase: 06-timeline-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/components/SessionDetailPanel.vue
  - src/client/components/GanttBar.vue
  - src/client/components/GanttChart.vue
  - src/client/components/GanttSwimlane.vue
  - src/client/pages/TimelinePage.vue
autonomous: true

must_haves:
  truths:
    - "Clicking a session bar populates a detail panel above the Gantt chart with session ID, ticket, branch, project, working time, wall-clock span, message count, and idle gap count"
    - "The detail panel shows a 'Select a session' placeholder when no session is selected"
    - "The selected session bar has a visible highlight state distinguishing it from unselected bars"
    - "Tooltip hover is removed from session bars (replaced by the detail panel)"
  artifacts:
    - path: "src/client/components/SessionDetailPanel.vue"
      provides: "Persistent detail panel showing session info"
      min_lines: 40
    - path: "src/client/components/GanttBar.vue"
      provides: "Clickable bar with selected highlight, no tooltip"
    - path: "src/client/pages/TimelinePage.vue"
      provides: "selectedSession state + detail panel placement"
  key_links:
    - from: "src/client/components/GanttBar.vue"
      to: "TimelinePage.vue"
      via: "emit('select', session) bubbled through GanttSwimlane and GanttChart"
      pattern: "emit.*select"
    - from: "src/client/pages/TimelinePage.vue"
      to: "src/client/components/SessionDetailPanel.vue"
      via: ":session prop binding"
      pattern: "selectedSession"
---

<objective>
Build a persistent detail panel above the Gantt chart that replaces tooltip hover on session bars. Clicking a bar selects it, highlights it, and populates the panel with session details.

Purpose: Replaces the single-line tooltip with an AWS Console-style detail panel — always visible, click-to-populate, richer information display.
Output: SessionDetailPanel.vue component, click-to-select wiring through GanttBar → GanttSwimlane → GanttChart → TimelinePage, tooltip removal.
</objective>

<execution_context>
@/home/meckert/.claude/get-shit-done/workflows/execute-plan.md
@/home/meckert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/client/components/GanttBar.vue
@src/client/components/GanttChart.vue
@src/client/components/GanttSwimlane.vue
@src/client/pages/TimelinePage.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionDetailPanel and wire click-to-select through bar → swimlane → chart → page</name>
  <files>
    src/client/components/SessionDetailPanel.vue
    src/client/components/GanttBar.vue
    src/client/components/GanttSwimlane.vue
    src/client/components/GanttChart.vue
    src/client/pages/TimelinePage.vue
  </files>
  <action>
    **SessionDetailPanel.vue** — New component. AWS Console-style persistent panel:
    - Props: `session` (Object, default null), `projectName` (String, default '')
    - When `session` is null: render a muted placeholder div with text "Select a session to view details"
    - When `session` is provided: render a horizontal key-value grid showing:
      - Session ID (first 12 chars + "...")
      - Ticket (if present)
      - Branch (if present)
      - Project (projectName prop)
      - Working Time (format: `XX min` from workingTimeMs / 60000, rounded)
      - Wall-Clock Span (format start time – end time using toLocaleTimeString 'en-US' with hour:'2-digit', minute:'2-digit')
      - Messages (messageCount)
      - Idle Gaps (idleGaps.length)
    - Styling: border-bottom, background var(--color-bg-secondary), padding var(--spacing-sm) var(--spacing-md). Grid layout: `display: grid; grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); gap: var(--spacing-sm)`. Each item has a `.detail-label` (font-size-xs, color-muted, text-transform uppercase, letter-spacing 0.05em) above a `.detail-value` (font-size-sm, color-heading, font-weight 500).

    **GanttBar.vue** — Modify:
    1. Remove the `<AppTooltip>` wrapper entirely. The root element becomes the `.gantt-bar` div directly.
    2. Remove the `import AppTooltip` and the `tooltipContent` computed.
    3. Add prop: `selected` (Boolean, default false).
    4. Add emit: `defineEmits(['select'])`.
    5. Add `@click="emit('select', session)"` on the `.gantt-bar` div.
    6. Add CSS class binding `:class="{ selected }"` on `.gantt-bar`.
    7. Add CSS rule `.gantt-bar.selected { box-shadow: 0 0 0 2px var(--color-primary, #4e9af1); z-index: 2; }`.

    **GanttSwimlane.vue** — Modify:
    1. Add props: `selectedSessionId` (String, default null).
    2. Add emit: `defineEmits(['select'])`.
    3. Pass to GanttBar: `:selected="session.sessionId === selectedSessionId"` and `@select="emit('select', $event)"`.

    **GanttChart.vue** — Modify:
    1. Add props: `selectedSessionId` (String, default null).
    2. Add emit: `defineEmits(['select'])`.
    3. Pass to GanttSwimlane: `:selected-session-id="selectedSessionId"` and `@select="emit('select', $event)"`.

    **TimelinePage.vue** — Modify:
    1. Import SessionDetailPanel.
    2. Add state: `const selectedSession = ref(null)`.
    3. Add computed `selectedProjectName` that finds the project displayName for the selectedSession from colorizedProjects.
    4. Place `<SessionDetailPanel :session="selectedSession" :project-name="selectedProjectName" />` inside `.timeline-content`, ABOVE the filter-bar.
    5. Add `@select="onSelectSession"` on GanttChart. `selectedSessionId` prop: `:selected-session-id="selectedSession?.sessionId"`.
    6. `onSelectSession(session)` — sets `selectedSession.value = session`. If clicking the already-selected bar, deselects: `selectedSession.value = selectedSession.value?.sessionId === session.sessionId ? null : session`.
    7. Clear selectedSession on date change: in the `watch(() => route.query.date, ...)` callback, add `selectedSession.value = null` before `fetchTimeline()`.
  </action>
  <verify>
    Run `npx vite build --config src/client/vite.config.js` (or the project's build command) — build succeeds with no errors. Visually: clicking a bar shows details in the panel above the chart, no tooltip on hover, selected bar has blue outline.
  </verify>
  <done>
    Clicking any session bar populates the detail panel with all 8 fields. Placeholder "Select a session" shows when nothing is selected. Selected bar has a box-shadow highlight. No tooltip on hover. Date navigation clears the selection.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `cd /home/claude/cctimereporter && node_modules/.bin/vite build --config src/client/vite.config.js`
2. Detail panel renders placeholder text when no session selected
3. Clicking a GanttBar emits select → bubbles up → populates detail panel
4. Selected bar has visible highlight (box-shadow)
5. AppTooltip import and usage removed from GanttBar.vue
6. Navigating dates clears the selected session
</verification>

<success_criteria>
All four must-have truths are satisfied: click-to-detail works, placeholder shown, highlight state visible, tooltip removed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-timeline-polish/06-01-SUMMARY.md`
</output>
