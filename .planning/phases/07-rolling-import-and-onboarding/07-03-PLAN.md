---
phase: 07-rolling-import-and-onboarding
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/client/pages/TimelinePage.vue
autonomous: true

must_haves:
  truths:
    - "A user who has never imported sees a welcome screen with tool explanation and a prominent Import button — not the generic empty-date view"
    - "A returning user with sessions but none on the selected date sees 'No sessions found for {date}' with a hint to try a different date"
    - "The welcome screen explains that the first import covers the last 30 days and may take a moment"
    - "The existing indeterminate progress bar in the toolbar is visible during import triggered from the welcome screen"
  artifacts:
    - path: "src/client/pages/TimelinePage.vue"
      provides: "Welcome state (totalSessions===0) and empty-date state (projects.length===0) as separate v-else-if branches"
      contains: "timeline-welcome"
  key_links:
    - from: "src/client/pages/TimelinePage.vue"
      to: "src/server/routes/timeline.js"
      via: "timelineData.totalSessions from API response"
      pattern: "totalSessions"
---

<objective>
Split the existing empty state in TimelinePage.vue into a first-time welcome screen and a returning-user empty-date message. Build the production frontend.

Purpose: First-time users get orientation and a clear call-to-action (IMP-03). The welcome text sets expectations for import duration (IMP-04). Returning users get a helpful hint instead of a confusing import button on an empty date.
Output: Two distinct empty states in TimelinePage.vue. Built production dist.
</objective>

<execution_context>
@/home/meckert/.claude/get-shit-done/workflows/execute-plan.md
@/home/meckert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-rolling-import-and-onboarding/07-RESEARCH.md
@.planning/phases/07-rolling-import-and-onboarding/07-02-SUMMARY.md
@src/client/pages/TimelinePage.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split empty state into welcome and empty-date states</name>
  <files>src/client/pages/TimelinePage.vue</files>
  <action>
    In the template section, replace the existing empty state block (the `v-else-if="timelineData && timelineData.projects.length === 0"` div) with TWO blocks. The welcome block MUST come FIRST in the v-else-if chain since `totalSessions === 0` implies `projects.length === 0` (both would be true, Vue processes top-down).

    1. Insert the welcome state BEFORE the existing empty state:
       ```html
       <!-- First-time welcome: no sessions ever imported -->
       <div v-else-if="timelineData && timelineData.totalSessions === 0" class="timeline-welcome">
         <h2>Welcome to CC Time Reporter</h2>
         <p>This tool scans your Claude Code session transcripts and shows them as a visual timeline grouped by project.</p>
         <p class="timeline-welcome-hint">Your first import covers the last 30 days of sessions. It may take a moment.</p>
         <AppButton variant="primary" size="lg" :loading="importRunning" @click="triggerImport">
           Import Sessions
         </AppButton>
       </div>
       ```

    2. Update the existing empty state block to be the empty-date state (for returning users with no sessions on the selected date). Change its content:
       ```html
       <!-- Empty date: sessions exist but none on this date -->
       <div v-else-if="timelineData && timelineData.projects.length === 0" class="timeline-empty">
         <p>No sessions found for <strong>{{ selectedDate }}</strong>.</p>
         <p class="timeline-empty-hint">Try navigating to a different date.</p>
       </div>
       ```
       Remove the AppButton from the empty-date state. Returning users know about import; they don't need a button on every empty date.

    3. Add CSS for the welcome state inside the `<style scoped>` section:
       ```css
       .timeline-welcome {
         display: flex;
         flex-direction: column;
         align-items: center;
         gap: var(--spacing-md);
         padding: var(--spacing-xl) var(--spacing-lg);
         text-align: center;
         max-width: 480px;
         margin: 0 auto;
       }

       .timeline-welcome h2 {
         color: var(--color-heading);
         font-size: var(--font-size-xl, 1.5rem);
         margin: 0;
       }

       .timeline-welcome p {
         color: var(--color-muted);
         margin: 0;
         line-height: 1.6;
       }

       .timeline-welcome-hint {
         font-size: var(--font-size-sm);
         opacity: 0.8;
       }

       .timeline-empty-hint {
         font-size: var(--font-size-sm);
         opacity: 0.7;
       }
       ```

    The toolbar with `AppProgressBar` (indeterminate mode) is already rendered above all states including the welcome state. When `triggerImport()` sets `importRunning = true`, the progress bar becomes visible in the toolbar automatically. No additional progress UI needed (IMP-04).
  </action>
  <verify>
    `grep -n 'timeline-welcome\|totalSessions' src/client/pages/TimelinePage.vue` — should show the welcome div and the totalSessions check. Confirm the welcome `v-else-if` appears before the empty-date `v-else-if` in line order.
  </verify>
  <done>
    Two distinct states: welcome (totalSessions === 0) with tool explanation + import CTA, and empty-date (projects.length === 0 but totalSessions > 0) with date hint. Empty-date state has no AppButton (removed). Welcome state explains 30-day window and import timing. Progress bar in toolbar provides feedback during import.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build production frontend</name>
  <files>dist/</files>
  <action>
    Run the Vite production build:
    ```bash
    npm run build
    ```
    This compiles the Vue frontend with the welcome state changes into the `dist/` directory that the Fastify server serves as static files.
  </action>
  <verify>
    `ls dist/index.html` exists. `grep 'Welcome to CC Time Reporter' dist/assets/*.js` — should find the welcome text in the compiled JS bundle (may be minified but the string literal should be present).
  </verify>
  <done>
    Production build completed with welcome and empty-date states compiled into dist/.
  </done>
</task>

</tasks>

<verification>
1. `grep 'timeline-welcome' src/client/pages/TimelinePage.vue` shows the welcome state div and CSS
2. `grep 'totalSessions' src/client/pages/TimelinePage.vue` shows the v-else-if check
3. The welcome `v-else-if` (totalSessions === 0) comes BEFORE the empty-date `v-else-if` (projects.length === 0) in the template
4. `ls dist/index.html` exists after build
5. Delete `~/.cctimereporter/data.db`, start server, open browser — should see welcome screen (not generic empty state)
6. After importing, navigate to a date with no sessions — should see "No sessions found" with hint (not the welcome screen)
</verification>

<success_criteria>
First-time users see a welcome screen explaining the tool with a clear import CTA. Returning users on an empty date see a date-specific message with navigation hint. The welcome text sets expectations for first-import timing. Production dist is built and ready to serve.
</success_criteria>

<output>
After completion, create `.planning/phases/07-rolling-import-and-onboarding/07-03-SUMMARY.md`
</output>
