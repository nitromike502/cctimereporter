---
phase: 09-day-summary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/components/DaySummary.vue
  - src/client/pages/TimelinePage.vue
autonomous: true

must_haves:
  truths:
    - "A total working time figure is visible below the Gantt chart for the selected day"
    - "A per-project table shows each project with session count and working time, sorted by working time descending"
    - "A per-ticket table shows each ticket with session count and working time, with null-ticket sessions grouped as a single (untracked) row"
    - "A per-branch table shows each branch with session count and working time, with null-branch sessions grouped as a single (untracked) row"
  artifacts:
    - path: "src/client/components/DaySummary.vue"
      provides: "Day summary component with total time and tabbed breakdowns"
      contains: "TabsRoot"
    - path: "src/client/pages/TimelinePage.vue"
      provides: "DaySummary integration after GanttChart"
      contains: "DaySummary"
  key_links:
    - from: "src/client/pages/TimelinePage.vue"
      to: "src/client/components/DaySummary.vue"
      via: "DaySummary component with projects prop bound to timelineData.projects"
      pattern: "DaySummary.*projects.*timelineData\\.projects"
    - from: "src/client/components/DaySummary.vue"
      to: "reka-ui"
      via: "TabsRoot, TabsList, TabsTrigger, TabsContent imports"
      pattern: "import.*TabsRoot.*from 'reka-ui'"
---

<objective>
Create the DaySummary component with total working time and tabbed per-project, per-ticket, and per-branch breakdowns, and integrate it into TimelinePage below the Gantt chart.

Purpose: Users need to see a breakdown of exactly how their working time was spent that day — by project, ticket, and branch — without scrolling through individual sessions.
Output: New `DaySummary.vue` component and 2-line integration in `TimelinePage.vue`.
</objective>

<execution_context>
@/home/meckert/.claude/get-shit-done/workflows/execute-plan.md
@/home/meckert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-day-summary/09-RESEARCH.md
@src/client/pages/TimelinePage.vue
@src/client/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DaySummary.vue component</name>
  <files>src/client/components/DaySummary.vue</files>
  <action>
Create `src/client/components/DaySummary.vue` with the following structure:

**Props:** `projects` (Array, required) — the full `timelineData.projects` array (NOT visibleProjects).

**Computed properties:**
- `allSessions`: flatten all sessions across all projects via `props.projects.flatMap(p => p.sessions)`
- `totalWorkingMs`: sum of `workingTimeMs` across all sessions (use `?? 0` for safety)
- `projectRows`: per-project aggregation — map each project to `{ displayName, sessionCount, workingTimeMs }`, sorted by workingTimeMs descending
- `ticketRows`: group sessions by `session.ticket || null` using a Map, sort ticketed entries by workingTimeMs desc, null-key group last. Output shape: `{ ticket, sessionCount, workingTimeMs }`
- `branchRows`: same pattern as ticketRows but keyed on `session.branch || null`. Output shape: `{ branch, sessionCount, workingTimeMs }`

**Helper function (local, not exported):**
```javascript
function formatWorkingTime(ms) {
  const totalMin = Math.round(ms / 60000)
  if (totalMin < 60) return `${totalMin} min`
  const h = Math.floor(totalMin / 60)
  const m = totalMin % 60
  return m === 0 ? `${h}h` : `${h}h ${m}m`
}
```

Use a shared `groupBy(sessions, keyFn)` helper that returns a Map to avoid duplicating the ticket/branch grouping logic.

**Template structure:**
1. Root `<div class="day-summary">`
2. Total working time line: `Total working time: <strong>{{ formatWorkingTime(totalWorkingMs) }}</strong>`
3. `<TabsRoot default-value="project">` wrapping:
   - `<TabsList class="tabs-list">` with three `<TabsTrigger>` buttons: "By Project" (value="project"), "By Ticket" (value="ticket"), "By Branch" (value="branch")
   - Three `<TabsContent>` panels, each containing a `<table class="summary-table">` with:
     - Project tab: columns Project, Sessions, Working Time — iterate `projectRows`, key on `row.displayName`
     - Ticket tab: columns Ticket, Sessions, Working Time — iterate `ticketRows`, key on `row.ticket ?? '__none__'`, display `row.ticket ?? '(untracked)'`
     - Branch tab: columns Branch, Sessions, Working Time — iterate `branchRows`, key on `row.branch ?? '__none__'`, display `row.branch ?? '(untracked)'`

**Imports:** `{ computed } from 'vue'` and `{ TabsRoot, TabsList, TabsTrigger, TabsContent } from 'reka-ui'`

**Scoped styles using design tokens from tokens.css:**

- `.day-summary`: `margin-top: var(--spacing-lg)`, `padding-top: var(--spacing-md)`, `border-top: 1px solid var(--color-border)`
- `.summary-total`: `font-size: var(--font-size-md)`, `color: var(--color-heading)`, `margin-bottom: var(--spacing-md)`
- `.tabs-list`: `display: flex`, `gap: 0`, `border-bottom: 1px solid var(--color-border)`, `margin-bottom: var(--spacing-sm)`
- `.tab-trigger`: `padding: var(--spacing-xs) var(--spacing-md)`, `background: none`, `border: none`, `border-bottom: 2px solid transparent`, `font-size: var(--font-size-sm)`, `font-family: var(--font-family)`, `color: var(--color-muted)`, `cursor: pointer`, `transition: color var(--transition-fast), border-color var(--transition-fast)`
- `.tab-trigger[data-state="active"]`: `color: var(--color-heading)`, `border-bottom-color: var(--color-primary)`
- `.tab-trigger:hover:not([data-state="active"])`: `color: var(--color-body-text)`
- `.summary-table`: `width: 100%`, `border-collapse: collapse`, `font-size: var(--font-size-sm)`
- `.summary-table th`: `text-align: left`, `padding: var(--spacing-xs) var(--spacing-sm)`, `color: var(--color-muted)`, `font-weight: 500`, `border-bottom: 1px solid var(--color-border)`
- `.summary-table td`: `padding: var(--spacing-xs) var(--spacing-sm)`, `border-bottom: 1px solid var(--color-border-subtle, var(--color-border))`
- Right-align the Sessions and Working Time columns (both th and td) for numeric readability.
  </action>
  <verify>File exists at `src/client/components/DaySummary.vue`, contains TabsRoot import, contains all three computed properties (projectRows, ticketRows, branchRows), contains formatWorkingTime function, and has scoped style block with data-state="active" rule.</verify>
  <done>DaySummary.vue is a self-contained component that computes total time, per-project, per-ticket, and per-branch breakdowns from a projects prop, displayed in accessible Reka UI tabs with design-token-consistent styling.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate DaySummary into TimelinePage</name>
  <files>src/client/pages/TimelinePage.vue</files>
  <action>
In `src/client/pages/TimelinePage.vue`:

1. Add import in the `<script setup>` block alongside other component imports:
   ```javascript
   import DaySummary from '../components/DaySummary.vue'
   ```

2. Add the component in the template, inside the `v-else-if="timelineData"` block (the `.timeline-content` div), AFTER the `<GanttChart>` component and its surrounding elements. Place it before the closing `</div>` of `.timeline-content`:
   ```vue
   <DaySummary :projects="timelineData.projects" />
   ```

CRITICAL: Pass `timelineData.projects` (unfiltered), NOT `visibleProjects`. The day summary must show the complete day's time accounting regardless of which projects are hidden in the Gantt chart filter.
  </action>
  <verify>Grep `TimelinePage.vue` for `DaySummary` — must appear in both the import and the template. Grep for `timelineData.projects` in the DaySummary binding — must NOT reference `visibleProjects`.</verify>
  <done>DaySummary component is imported and rendered in TimelinePage after the Gantt chart, receiving the full unfiltered projects array.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors (Vue compilation succeeds)
2. `grep -c 'TabsRoot' src/client/components/DaySummary.vue` returns 1+
3. `grep 'DaySummary' src/client/pages/TimelinePage.vue` shows both import and template usage
4. `grep 'timelineData.projects' src/client/pages/TimelinePage.vue` confirms DaySummary receives unfiltered data
</verification>

<success_criteria>
- DaySummary.vue exists and renders total working time plus three tab panels (project, ticket, branch)
- Each tab panel contains a table with name, session count, and formatted working time columns
- Null tickets and branches are grouped into a single "(untracked)" row per table
- TimelinePage renders DaySummary below the Gantt chart with the full projects array
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-day-summary/09-01-SUMMARY.md`
</output>
