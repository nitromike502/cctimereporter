---
phase: 17-session-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.js
  - src/db/index.js
  - src/importer/db-writer.js
  - src/server/routes/sessions.js
  - src/server/index.js
  - src/server/routes/timeline.js
autonomous: true

must_haves:
  truths:
    - "After re-import, user-set session names and ticket overrides are preserved (not wiped)"
    - "PATCH /api/sessions/:id accepts userLabel and userTicket, stores them as user_label and user_ticket"
    - "Timeline API returns userLabel and userTicket fields for each session"
    - "Database migrates cleanly from v5 to v6 adding user_label and user_ticket columns"
  artifacts:
    - path: "src/db/schema.js"
      provides: "SCHEMA_VERSION 6, MIGRATION_V5_TO_V6 constant, user_label/user_ticket in DDL"
      contains: "MIGRATION_V5_TO_V6"
    - path: "src/db/index.js"
      provides: "migrateV5toV6 function, updated migration chain"
      contains: "migrateV5toV6"
    - path: "src/importer/db-writer.js"
      provides: "upsertSession using INSERT ON CONFLICT DO UPDATE, omitting user columns"
      contains: "ON CONFLICT"
    - path: "src/server/routes/sessions.js"
      provides: "PATCH /api/sessions/:id endpoint"
      exports: ["sessionsRoute"]
    - path: "src/server/routes/timeline.js"
      provides: "userLabel and userTicket in session response objects"
      contains: "user_label"
  key_links:
    - from: "src/server/index.js"
      to: "src/server/routes/sessions.js"
      via: "app.register(sessionsRoute)"
      pattern: "sessionsRoute"
    - from: "src/importer/db-writer.js"
      to: "sessions table"
      via: "ON CONFLICT DO UPDATE omitting user_label/user_ticket"
      pattern: "ON CONFLICT.*DO UPDATE"
---

<objective>
Add database schema support for user-editable session fields, fix the upsert to preserve user data across re-imports, create a PATCH API endpoint for updating user fields, and include user fields in the timeline API response.

Purpose: Backend foundation for session editing — without this, the frontend has nowhere to save or read user edits.
Output: Schema v6 with user_label/user_ticket columns, safe upsert, PATCH /api/sessions/:id endpoint, updated timeline response.
</objective>

<execution_context>
@/home/meckert/.claude/get-shit-done/workflows/execute-plan.md
@/home/meckert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-session-editing/17-RESEARCH.md
@src/db/schema.js
@src/db/index.js
@src/importer/db-writer.js
@src/server/index.js
@src/server/routes/timeline.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration v5->v6 and upsert fix</name>
  <files>src/db/schema.js, src/db/index.js, src/importer/db-writer.js</files>
  <action>
  1. In `src/db/schema.js`:
     - Add `user_label TEXT` and `user_ticket TEXT` columns to the sessions CREATE TABLE in SCHEMA_DDL (after `first_prompt`)
     - Bump SCHEMA_VERSION from 5 to 6
     - Add new migration constant:
       ```
       export const MIGRATION_V5_TO_V6 = `
       ALTER TABLE sessions ADD COLUMN user_label TEXT;
       ALTER TABLE sessions ADD COLUMN user_ticket TEXT;
       `;
       ```

  2. In `src/db/index.js`:
     - Import MIGRATION_V5_TO_V6 from schema.js
     - Add `migrateV5toV6(db)` function following the existing pattern (calls `runMigration(db, MIGRATION_V5_TO_V6)`)
     - Update the migration chain in `openDatabase()`: add `else if (existingVersion === 5)` case that calls `migrateV5toV6(db)`, and chain `migrateV5toV6(db)` into all earlier version cases (1, 2, 3, 4)

  3. In `src/importer/db-writer.js`:
     - Convert `upsertSession` from `INSERT OR REPLACE` to `INSERT ... ON CONFLICT(session_id) DO UPDATE SET`
     - In the ON CONFLICT UPDATE SET clause, list ALL import-managed columns (project_id, file_path, file_size, file_modified_at, working_branch, primary_ticket, summary, custom_title, slug, first_message_at, last_message_at, last_updated_at, message_count, user_message_count, assistant_message_count, tool_use_count, fork_count, real_fork_count, is_compacted, has_subagents, is_subagent, team_name, agent_name, first_prompt) using `excluded.column_name` syntax
     - Deliberately OMIT user_label and user_ticket from the ON CONFLICT clause so they are preserved across re-imports
     - Update the JSDoc comment to explain the ON CONFLICT approach and why user columns are omitted
  </action>
  <verify>
  - `node -e "import('./src/db/schema.js').then(m => { console.log('VERSION:', m.SCHEMA_VERSION); console.log('HAS_V5_V6:', !!m.MIGRATION_V5_TO_V6) })"` prints VERSION: 6, HAS_V5_V6: true
  - `grep -c 'ON CONFLICT' src/importer/db-writer.js` returns 1
  - `grep -c 'INSERT OR REPLACE' src/importer/db-writer.js` returns 0 (for the sessions upsert; import_log still uses INSERT OR REPLACE which is fine)
  - `grep 'user_label' src/db/schema.js` shows the column in both DDL and migration
  </verify>
  <done>Schema is at v6 with user_label and user_ticket columns. upsertSession uses INSERT ON CONFLICT DO UPDATE that preserves user-set columns across re-imports.</done>
</task>

<task type="auto">
  <name>Task 2: PATCH API endpoint and timeline response update</name>
  <files>src/server/routes/sessions.js, src/server/index.js, src/server/routes/timeline.js</files>
  <action>
  1. Create `src/server/routes/sessions.js`:
     - Export async function `sessionsRoute(fastify, opts)` following the pattern in other route files
     - Destructure `{ db }` from opts
     - Prepare the UPDATE statement ONCE outside the route handler (performance best practice from the codebase):
       ```
       const updateStmt = db.prepare(`
         UPDATE sessions
         SET user_label = $user_label, user_ticket = $user_ticket
         WHERE session_id = $session_id
       `);
       ```
     - Also prepare a SELECT statement for existence check:
       ```
       const findStmt = db.prepare('SELECT session_id FROM sessions WHERE session_id = ?');
       ```
     - Register `fastify.patch('/api/sessions/:id', ...)`:
       - Get sessionId from `request.params.id`
       - Get `{ userLabel, userTicket }` from `request.body ?? {}`
       - Normalize empty strings to null: `const label = userLabel || null; const ticket = userTicket || null;`
       - Check session exists with findStmt.get(sessionId); if not found, return 404 with `{ error: 'Session not found' }`
       - Run updateStmt with `{ $user_label: label, $user_ticket: ticket, $session_id: sessionId }`
       - Return `{ ok: true }`

  2. In `src/server/index.js`:
     - Import `sessionsRoute` from `./routes/sessions.js`
     - Register it: `app.register(sessionsRoute, { db })`

  3. In `src/server/routes/timeline.js`:
     - Add `s.user_label` and `s.user_ticket` to the SELECT list in `sessionStmt`
     - Add `userLabel: row.user_label` and `userTicket: row.user_ticket` to the `sessionObj` object (alongside the existing ticket, branch, summary, etc.)
  </action>
  <verify>
  - `node -e "import('./src/server/routes/sessions.js').then(m => console.log('export:', typeof m.sessionsRoute))"` prints "export: function"
  - `grep 'sessionsRoute' src/server/index.js` shows import and register lines
  - `grep 'user_label' src/server/routes/timeline.js` shows the column in SELECT
  - `grep 'userLabel' src/server/routes/timeline.js` shows the field in sessionObj
  </verify>
  <done>PATCH /api/sessions/:id endpoint exists and normalizes empty strings to null. Timeline API returns userLabel and userTicket for each session.</done>
</task>

</tasks>

<verification>
- Schema version is 6
- upsertSession uses ON CONFLICT, not INSERT OR REPLACE
- PATCH endpoint exists and is registered
- Timeline response includes userLabel and userTicket fields
- No import of user columns in the ON CONFLICT UPDATE SET clause
</verification>

<success_criteria>
- Database migrates from v5 to v6 without errors
- Re-import does not overwrite user_label or user_ticket values
- PATCH /api/sessions/:id returns 200 for valid sessions, 404 for invalid
- Timeline API response includes userLabel and userTicket per session
</success_criteria>

<output>
After completion, create `.planning/phases/17-session-editing/17-01-SUMMARY.md`
</output>
