# Milestone v1.1.0: Token Usage Tracking & Visualization

**Status:** ✅ SHIPPED 2026-05-09
**Phases:** 32-36
**Total Plans:** 8

## Overview

Users can see token usage counts in the session detail panel, day summary, CLI output, MCP tools, and a new `/tokens` line chart page showing token spend over time per session — with double-click drill-down to see exactly which messages caused a usage spike.

## Phases

### Phase 32: Data Foundation

**Goal:** Token usage fields are stored in SQLite per assistant message, schema auto-migrates from v9 to v10, and historical sessions are backfilled via re-import so there is real data to verify at every downstream layer.
**Depends on:** Phase 31 (schema v9, import pipeline)
**Plans:** 1 plan

Plans:
- [x] 32-01: Schema v10 migration (7 new columns on messages) and importer extraction (three-place update: schema.js, db-writer.js, importer/index.js)

**Verification:** passed (6/6 truths verified, see 32-VERIFICATION.md)

---

### Phase 33: Service, API, and Token Queries

**Goal:** Token aggregates are queryable via a dedicated backend service and a new API endpoint, with correct filtering to exclude sidechain and fork-branch messages — resolving "what total tokens means" at the SQL layer before any UI commits to a display format.
**Depends on:** Phase 32
**Requirements:** DISP-01, DISP-02, DISP-03
**Plans:** 2 plans

Plans:
- [x] 33-01: Token service factory and API route (createTokensService + GET /api/tokens with sidechain/fork exclusion and JS-computed cache hit rate)
- [x] 33-02: Vue frontend token display (TimelinePage non-blocking fetch, SessionDetailPanel breakdown, DaySummary day total)

**Verification:** passed (9/9 truths verified, retroactive 2026-05-09)

---

### Phase 34: CLI and MCP Extension

**Goal:** Token totals appear in structured CLI JSON output and MCP tool responses, extending existing outputs without breaking current consumers.
**Depends on:** Phase 33
**Requirements:** DISP-04, DISP-05, DISP-06, DISP-07
**Plans:** 1 plan

Plans:
- [x] 34-01: CLI and MCP token enrichment (summary, sessions, get_day_summary, get_sessions — additive only, sessionTokenMap pattern)

**Verification:** passed (6/6 truths verified, retroactive 2026-05-09)

---

### Phase 35: Tokens Chart Page

**Goal:** A new `/tokens` page is accessible from the app navigation and shows a line chart with one line per session and an aggregate line, with cumulative/per-message toggle, per-session line visibility toggle.
**Depends on:** Phase 33
**Requirements:** CHART-01, CHART-02, CHART-03, CHART-04, CHART-05, CHART-06
**Plans:** 2 plans

Plans:
- [x] 35-01: chart.js + vue-chartjs install; TokensPage scaffold with date nav, app header nav, route registration, shared project-colors utility
- [x] 35-02: Line chart implementation (per-session lines, cumulative/per-message toggle, custom HTML legend, session detail on click, dark mode reactive options) — note: implementation evolved during rework commit (`dba4fc2`) to Session Totals stacked bars + Per Message line chart with time-of-day x-axis and project-level visibility checkboxes

**Verification:** human_needed (5/5 verified at code level; 3 UX evolutions documented and accepted; visual rendering in light/dark themes requires human smoke test, retroactive 2026-05-09)

---

### Phase 36: Tokens Chart Message Drill-Down (added mid-milestone, bonus scope)

**Goal:** Double-clicking a point on the Per Message line chart opens a modal showing the messages from that session's time bucket, with token counts on assistant messages, enabling users to understand exactly what caused a token usage spike.
**Depends on:** Phase 35
**Plans:** 2 plans

Plans:
- [x] 36-01: Backend timestamp-range query mode for messages API (from/to params, output_tokens in response)
- [x] 36-02: Frontend double-click handler on line chart, bucket-to-timestamp resolution, SessionMessagesModal time-range mode with token display

**Verification:** passed (8/8 truths verified, see 36-VERIFICATION.md)

---

## Milestone Summary

**Decimal Phases:** None.

**Bonus Phase:** Phase 36 was added on 2026-04-10 mid-milestone (not in original v1.1.0 requirements). Provides drill-down from chart bucket to message detail with inline token counts.

**Key Decisions:**

- Token columns on messages table (not sessions) — import raw, derive at query time; avoids repeating the dead tool_use_count mistake
- chart.js 4.5.1 + vue-chartjs 5.3.3 as devDependencies only — bundled by Vite, not runtime npm deps; avoids inflating CLI node_modules
- Sidechain exclusion (is_sidechain=0) as default for all token aggregates — parent-only totals prevent 2-5x double-count for subagent users
- Fork branch exclusion (is_fork_branch=0) for "actual spend" totals — abandoned branches had real API calls but should not count as user spend
- Re-import via import_log deletion for last 30 days during v10 migration — automatic backfill without blocking startup or forcing --all on heavy users
- NULL not 0 for non-assistant token columns (32-01) — avoids misleading zero aggregates in downstream queries
- Ephemeral cache tiers at usage.cache_creation.ephemeral_* not top-level (32-01) — matches actual JSONL schema
- Agent sidechain messages also extract token data (32-01) — preserves actual spend tracking for subagent users
- Shared project color utility in src/client/utils/project-colors.js (35-01) — extracted from TimelinePage, ensures consistent colors across Timeline and Tokens pages
- App.vue persistent nav excludes /components route (35-01) — dev tool, not user-facing navigation
- createTokensService factory pattern (33-01) — prepared statements at factory time, same as timeline/sessions services
- SQLite aggregate null detection (33-01) — aggregate queries always return 1 row; check all individual columns null to detect "no data" vs "no session"
- Supplementary fetch pattern (33-02) — token fetch is fire-and-forget alongside timeline fetch; failures silently null out tokenData, never propagate to timeline
- formatTokenCount null/zero returns em dash in detail panel, null in DaySummary
- sessionId stripped from tokens sub-object in CLI/MCP session responses (34-01) — redundant since sessionId is already on the parent session object
- CLI lazy import pattern for token service (34-01) — defers prepared-statement setup until command runs
- mapRowWithTokens variant for time-range path (36-01) — separate mapper prevents outputTokens leaking into existing response shape
- No from/to validation in HTTP layer (36-01) — invalid ISO strings safely return 0 rows from SQLite timestamp comparison
- Native dblclick + getElementsAtEventForMode for chart drill-down (36-02) — Chart.js has no built-in onDblClick
- timelineBucketState computed exposes bucketStarts/bucketMap (36-02) — single source of truth for both chart rendering and double-click handler
- isBucketView API flag guards token display in modal (36-02) — API is authoritative
- UX evolution during 35-02 rework (`dba4fc2`, 2026-04-10): "Cumulative ↔ Per Message" toggle became "Session Totals ↔ Per Message" (stacked bars vs time-of-day line chart); per-session line visibility became per-project checkbox filtering; aggregate "all sessions" line subsumed by Session Totals bar view; message-index x-axis became time-of-day with configurable bucket interval (foundation for Phase 36 drill-down)

**Issues Resolved:**

- Project color collisions on tokens page fixed via assignment-based dedup in project-colors.js (`dba4fc2`)
- Project color carryover between dates fixed via resetProjectColors() on date change (`dba4fc2`)

**Issues Deferred / Tech Debt:**

- CHART-04 literal "all sessions" aggregate overlay line on Per Message chart (substituted with Session Totals bar view)
- CHART-05 literal "Cumulative ↔ Per Message" wording (renamed to "Session Totals ↔ Per Message")
- CHART-06 per-session visibility granularity (implemented as per-project)
- VERIFICATION.md skipped at completion time for Phases 33, 34, 35 (verified retroactively during milestone audit, 2026-05-09)
- 35-02-SUMMARY.md was backfilled 2026-05-09 from commits edf0284 + dba4fc2

**Technical Debt Incurred:**

- Phase 35 visual rendering in light/dark themes never received an explicit human smoke test at completion time. Phase 36 shipped on top providing indirect functional confirmation; no known visual issues.
- Token data for sessions before schema v10 migration depends on user re-running import; the v10 migration auto-flags last 30 days for re-import but heavier users on older data must run `--all`.

---

*For current project status, see .planning/ROADMAP.md*
